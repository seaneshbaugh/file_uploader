{
    "files" : [
        {
            "name" : "<%= params['file'][:filename] %>",
            "type" : "<%= params['file'][:type] %>",
            "size" : <%= params['file'][:tempfile].size %>
        }
    ]
}
